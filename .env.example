# Ollama (default: local). Must match a name from `ollama list`.
# If this model is missing, the app falls back to the first installed model at startup.
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
